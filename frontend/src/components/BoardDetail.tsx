/**
 * BoardDetail — board detail page placeholder.
 * Full implementation in Task 7.
 */
interface Props {
  id: number;
}

export default function BoardDetail({ id }: Props) {
  return <div data-testid="board-detail">BoardDetail placeholder — board {id}</div>;
}
